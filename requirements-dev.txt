# Development & test dependencies (not needed to RUN the server in production).
# Install with:  .venv/bin/pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
respx==0.23.1
freezegun==1.5.5
ruff==0.16.6
